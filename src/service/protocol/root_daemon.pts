

msg ReportActivities
{
    request();
    response(std::vector< network::ActivityID > activities);
}

msg ListWorkers
{
    request();
    response(int threads);
}

msg PipelineStartJobs
{
    request(mega::pipeline::Pipeline::ID pipelineID, mega::pipeline::Configuration configuration, network::ActivityID rootActivityID);
    response(std::vector< network::ActivityID > jobs);
}
