
msg GetVersion
{
    request();
    response(std::string version);
}

msg ListHosts
{
    request();
    response(std::vector< std::string > hosts);
}

msg ListActivities
{
    request();
    response(std::vector< network::ConversationID > activities);
}

msg PipelineRun
{
    request(mega::pipeline::Configuration configuration);
    response(mega::network::PipelineResult result);
}

msg Shutdown
{
    request();
    response();
}
