
msg GetVersion
{
    request();
    response(std::string version);
}

msg ListHosts
{
    request();
    response(std::vector< std::string > hosts);
}

msg runTestPipeline
{
    request();
    response(std::string result);
}
