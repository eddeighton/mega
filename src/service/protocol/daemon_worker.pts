

msg ReportActivities
{
    request();
    response(std::vector< ActivityID > activities);
}

msg ListThreads
{
    request();
    response(int threads);
}

msg PipelineStartJobs
{
    request(mega::pipeline::Pipeline::ID pipelineID,ActivityID rootActivityID);
    response(std::vector< ActivityID > jobs);
}

