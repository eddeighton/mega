

msg GetStatus
{
    broadcast();
    response(mega::network::Status status);
}

msg GetNetworkStatus
{
    request();
    response(mega::network::Status status);
}

msg Ping
{
    request();
    response(std::string ping);
}
