



msg GetProject
{
    request();
    response(mega::network::Project project);
}

msg SetProject
{
    request(mega::network::Project project);
    response();
}

msg GetMegastructureInstallation
{
    request();
    response(mega::network::MegastructureInstallation install);
}
