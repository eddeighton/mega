



msg EnroleDaemon
{
    request();
    response(MP daemonMP);
}

msg EnroleLeafWithDaemon
{
    request(mega::network::Node::Type type);
    response(MP leafMP);
}

msg EnroleLeafWithRoot
{
    request(MP daemonMP);
    response(MP leafMP);
}

msg EnroleLeafDisconnect
{
    request(MP leafMP);
    response();
}
