
msg SimLockRead
{
    request(mega::MPO owningID,mega::MPO requestID);
    response(bool bSuccess);
}
msg SimLockWrite
{
    request(mega::MPO owningID,mega::MPO requestID);
    response(bool bSuccess);
}
msg SimLockRelease
{
    request(mega::MPO owningID,mega::MPO requestID);
    response();
}
msg SimClock
{
    request();
    response();
}
msg SimCreate
{
    request();
    response(mega::MPO mpo);
}
msg SimStart
{
    request();
    response();
}
msg SimDestroy
{
    request(mega::MPO mpo);
    response();
}
