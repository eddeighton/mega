


msg SimLockRead
{
    request(mega::MPO requestingMPO);
    response(bool bSuccess);
}
msg SimLockWrite
{
    request(mega::MPO requestingMPO);
    response(bool bSuccess);
}
msg SimLockRelease
{
    request(mega::MPO requestingMPO);
    response();
}
msg SimClock
{
    request();
    response();
}
msg SimCreate
{
    request();
    response(mega::MPO mpo);
}
msg SimStart
{
    request();
    response();
}
msg SimDestroy
{
    request();
    response();
}
