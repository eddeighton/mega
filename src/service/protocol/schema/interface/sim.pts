
msg SimLockRead
{
    request(mega::network::ConversationID owningID,mega::network::ConversationID requestID);
    response(bool bSuccess);
}
msg SimLockWrite
{
    request(mega::network::ConversationID owningID,mega::network::ConversationID requestID);
    response(bool bSuccess);
}
msg SimLockRelease
{
    request(mega::network::ConversationID owningID,mega::network::ConversationID requestID);
    response();
}
msg SimClock
{
    request();
    response();
}
msg SimDestroy
{
    request(mega::network::ConversationID requestID);
    response();
}
