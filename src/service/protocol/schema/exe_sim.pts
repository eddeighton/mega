

msg ExeSimDestroy
{
    request(mega::network::ConversationID requestID);
    response();
}
msg ExeSimReadLockAcquire
{
    request(mega::network::ConversationID requestID);
    response(bool bSuccess);
}
msg ExeSimWriteLockAcquire
{
    request(mega::network::ConversationID requestID);
    response(bool bSuccess);
}
msg ExeSimLockRelease
{
    request(mega::network::ConversationID requestID);
    response();
}
msg ExeExeClock
{
    request();
    response();
}
