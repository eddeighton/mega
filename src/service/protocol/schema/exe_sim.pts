
msg ExeSimReadLockAcquire
{
    request(mega::network::ConversationID simulationID);
    response(mega::TimeStamp timestamp);
}
msg ExeSimReadLockRelease
{
    request(mega::network::ConversationID simulationID);
    response();
}
msg ExeSimWriteLockAcquire
{
    request(mega::network::ConversationID simulationID);
    response(mega::TimeStamp timestamp);
}
msg ExeSimWriteLockRelease
{
    request(mega::network::ConversationID simulationID);
    response();
}

