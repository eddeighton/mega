

msg RootMPO
{
    request(network::Message request, mega::MPO mpo);
    response(network::Message response);
}

msg MPOMPODown
{
    request(network::Message request, mega::MPO mpo);
    response(network::Message response);
}

// special case for registering MPO and running simulation
msg RootSimRun
{
    request(mega::MPO mpo);
    response();
}

