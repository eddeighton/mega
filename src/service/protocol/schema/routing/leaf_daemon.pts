
msg TermRoot
{
    request(network::Message request);
    response(network::Message response);
}

msg ExeRoot
{
    request(network::Message request);
    response(network::Message response);
}

msg ToolRoot
{
    request(network::Message request);
    response(network::Message response);
}

msg LeafDaemon
{
    request(network::Message request);
    response(network::Message response);
}

msg LeafRoot
{
    request(network::Message request);
    response(network::Message response);
}
