

msg MPORoot
{
    // route to the leaf where MPO will be added to routing message to Root
    request(network::Message request);
    response(network::Message response);
}
msg MPOMPOUp
{
    // route using initiated conversation path
    request(network::Message request, mega::MPO mpo);
    response(network::Message response);
}

